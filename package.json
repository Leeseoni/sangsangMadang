{
  "scripts": {
    "dev": "sass scss:css --watch",
    "build": "sass scss:build --style=compressed --no-source-map"
  },
  "dependencies": {
    "pretendard": "^1.3.9",
    "sass": "^1.83.0"
  }
}
